# MQTT broker
MQTT_URL=5c5096ba5ead4ecf8917a53fc974ee70.s1.eu.hivemq.cloud:8883
MQTT_USERNAME=aquaguard
MQTT_PASSWORD=Gigatt02!
MQTT_CLIENT_ID=aquaguard-bridge

# Topic namespace; receivers publish under <prefix>/<deviceId>/...
TOPIC_PREFIX=aquaguard

# Firebase
FIREBASE_DATABASE_URL=https://your-project-default-rtdb.asia-southeast1.firebasedatabase.app
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# Retention: prune /readings/<id>/* older than this many days (0 disables)
READING_RETENTION_DAYS=30

# HTTP health endpoint
HEALTH_PORT=8080

LOG_LEVEL=info
